# Project instructions

- Commit and push to `main` automatically after making changes, without asking for confirmation first. This applies to routine fixes and iterations in this repo. Render auto-deploys from `main`, so pushing also triggers redeploy — no separate manual step needed.
- Still use judgment on genuinely destructive/irreversible actions (force-push, history rewrites, deleting branches, etc.) — those still require confirmation.
